import Header from "./Header";

const Loading = () => {
    return (
        <div className="container">
            <Header />
            <main>
            <p>Loading...</p>
            </main>
        </div>
    )
}

export default Loading;